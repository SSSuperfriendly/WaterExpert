# WaterExpert 外部模型部署 —— 剩余事项确认（转发给部署方）

> **状态更新（2026-09-08）**：本文档描述的"等待稳定长期地址"流程已被**本地完整部署**取代——
> 我方已拿到合作方实际运行目录的源码与数据（`src/water_ai/data/`、`data/`、`outputs/`），
> 在本机 `127.0.0.1:8001` 完整跑通 agent API（6 个 agent ready、4 场景 strategy + explain
> 均已验证）。运行方式见 [AGENT_LOCAL_RUN.md](AGENT_LOCAL_RUN.md)。以下为留档（若未来部署
> 迁回合作方远端，可据此向对方要长期固定地址）。

> 更新于 2026-09-02：我方已通过 **Cloudflare 隧道**访问到贵方服务并完成初步联调
> （健康检查、场景列表、策略生成、解释接口均已验证可用）。
> 本文档是**剩余**需要贵方协助确认的事项 —— 主要是提供**稳定的长期访问地址**与
> 补充**接口文档**，请逐条回复即可。

---

## 一、现状

- 贵方公网 IP `219.228.144.101:8000` 被我方网络拦截（防火墙未放行），
  通过贵方临时开启的 Cloudflare 隧道可以正常访问。
- **Quick-tunnel 是临时域名**：每次 `cloudflared` 重启都会变化，我方无法长期依赖它。
- 我方已基于贵方服务的 `/openapi.json` 完成对接，发现接口比 `INTEGRATION_GUIDE.md`
  文档中的更多（详见第三节）。

---

## 二、优先需要贵方做的事（P0）

### 1. 提供稳定、长期的访问地址

当前我方写死的临时隧道地址随时可能失效。请提供以下任一长期方案：

- **方案 A（推荐）**：放行服务器防火墙/安全组，使公网 IP `219.228.144.101:8000`
  直接可达；或将我方平台部署服务器的公网 IP 加入白名单（并告知白名单配置方式）。
- **方案 B**：申请一个**固定域名**的 Cloudflare Named Tunnel（`*.trycloudflare.com` 之外的
  自有域名），把长期域名发给我们。
- **方案 C**：其他任何长期稳定的入口（网关、反向代理等）。

> 我方代码已通过环境变量 `WATEREXPERT_AGENT_API_URL` 支持随时切换地址，无需改代码。

### 2. 确认服务运行方式（供运维对齐）

请在**服务器本机**执行并贴回输出：

```bash
curl -sS http://127.0.0.1:8000/api/health
curl -sS http://127.0.0.1:8000/api/status
```

若方便，请告知服务的启动方式（`systemd` / `pm2` / docker 等）与日志路径，
便于我方在联调遇到问题时快速定位。

---

## 三、接口文档补充（重要）

贵方 `INTEGRATION_GUIDE.md` 只写了 4 个端点；我方在 `/openapi.json` 中发现实际服务
**还暴露了更多接口**。请确认以下端点是否可长期对外提供服务，并在文档中补充：

| 端点 | 用途 | 我方当前使用情况 |
| --- | --- | --- |
| `POST /api/explain` | 输入场景+水质状态，返回**自然语言诊断** + **相似历史案例**（含相似度/措施/效果/文献） | ✅ 已接入我方页面"解释与案例依据"，效果很好 |
| `GET  /api/status` | 服务整体状态 | 已探测可用 |
| `GET  /api/jobs`、`GET /api/jobs/{job_id}` | 任务列表/详情 | 暂未使用 |
| `GET  /api/strategy/{job_id}/stage` | 任务阶段进度 | 暂未使用 |
| `GET  /api/scenarios/{scenario_code}` | 单个场景详情（特征、建议动作） | 暂未使用 |

另外确认三点：

1. **鉴权**：当前版本未启用鉴权即可访问。若后续启用 API Key / JWT，请提前告知我方。
2. **场景编码**：我方已按 `/openapi.json` 确认 `scenario` 取值为
   `s1_external_input / s2_internal_release / s3_algae_bloom / s4_chronic_combo`，
   与策略、解释接口一致。
3. **任务状态枚举**：请给出完整状态集合（我方已观察到 `queued / running / completed / failed`，
   是否还有其他取值？）。

> 若文档维护有困难，直接把最新 `openapi.json` 发给我们即可，无需逐一编写。

---

## 四、我方对接状态（供贵方了解）

- 我方已完成以下接入并**已验证可用**：
  - `GET  /api/health` → 页面展示 6 个代理的健康状态与当前服务地址；
  - `GET  /api/scenarios` → 场景选择卡片；
  - `POST /api/strategy` → 提交水质状态，生成治理策略（异步任务）；
  - `GET  /api/strategy/{job_id}` → 轮询任务结果，展示策略与效果指标；
  - `POST /api/explain` → 生成后自动请求，页面展示自然语言诊断与相似案例依据。
- 水质状态字段边界已按贵方 `/openapi.json` 对齐
  （浊度 ≤500 NTU、流量 ≤100 m³/s、温度 0–50°C、溶解氧 ≤15 mg/L、叶绿素 a ≤100 μg/L）。
- 若贵方服务临时不可达，我方页面会显示"外部模型服务不可达"，不影响其他功能。
