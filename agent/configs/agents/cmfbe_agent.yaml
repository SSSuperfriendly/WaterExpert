checkpoint_path: outputs/models/cmfbe_stgcn.pt
# No ``threshold_summary_path``. The critical levels travel in the request's
# knowledge context, sourced from the platform's threshold graph; CMFBE has no
# local fallback and reports ``threshold_source: unavailable`` rather than
# reading one. The snapshot this key pointed at disagreed with the platform on
# 4 of 10 levels (``wind_speed`` by 2.7x) and was deleted on 2026-09-13.
